#include<stdio.h>
#include<conio.h>

int main()
{
//	int *ptr;
//	ptr = (int*)malloc(sizeof(int)*4);
//	ptr = realloc(ptr,sizeof(int)*2);
//	printf("%d",sizeof(ptr));
//	return 0;

	FILE *fp;
	fp = fopen("hello","r");
	return 0;
}
