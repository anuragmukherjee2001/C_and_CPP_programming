#include<stdio.h>
#include<string.h>

int main(){

    char s1[100] = "Anurag";
    char s2[100] = "Mukherjee";
    char s3[54];
    char a[20];
    int o,b;

    // puts(strcat(s1,s2));

    // printf("The length of s1 is %d\n", strnlen(s1,100));
    // printf("The length of s2 is %d\n", strnlen(s2,100));

    // printf("The reversed string is %s \n",strrev(s1));
    // printf("The reversed string is %s \n",strrev(s2));

    // printf("the concatenation of two string is %s\n",strcat(s1,s2));

    // strset(a,4);
    // puts(a);

    return 0;
}